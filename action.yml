name: 'Test Gradle Action'
description: 'Test something'
inputs:
  args:  # id of input
    description: 'arguments of app'
    required: true
    default: 'no arguments'
outputs:
  random-number:
    description: "Random number"
    value: ${{ steps.random-number-generator.outputs.task_output }}
# runs:
#   using: 'docker'
#   image: 'Dockerfile'
#   args:
#     - ${{ inputs.args }}
# runs:
#   using: 'composite'
#   steps:
    
    - id: action_pre
      run: echo starting action
      shell: bash
    - id: build
      run: docker compose up -d my_api
      shell: bash
    - id: action_done_build
      run: echo done build
      shell: bash